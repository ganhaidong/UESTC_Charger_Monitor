# Caddy 反向代理配置示例
# 用途：让前端不再写端口，链接更干净；用域名时可自动 HTTPS。
# 安装：sudo apt install -y caddy
# 使用：把下面你需要的方案取消注释，存到 /etc/caddy/Caddyfile，然后 sudo systemctl enable --now caddy

# ──────────────────────────────────────────────────────────────
# 方案一：只用 IP，走 HTTP(80 端口) → 链接变成  http://118.24.139.8/
#   无需域名。若想再走 HTTPS，必须用下面的方案二。
# ──────────────────────────────────────────────────────────────
# :80 {
#     reverse_proxy 127.0.0.1:8765
# }

# ──────────────────────────────────────────────────────────────
# 方案二：域名 + 自动 HTTPS（推荐）→ 链接变成  https://你的域名/
#   前提：把 yourdomain.com 的 A 记录解析到 118.24.139.8。
#   Caddy 会自动申请并续期 Let's Encrypt 证书。
# ──────────────────────────────────────────────────────────────
# yourdomain.com {
#     reverse_proxy 127.0.0.1:8765
# }

# 说明：后端服务默认监听 0.0.0.0:8765。启用 Caddy 后建议把后端改成只监听本机，
#   避免 8765 也是明文暴露：
#   编辑 deploy/charger-monitor.service 的 ExecStart，把 --host 0.0.0.0 改成 --host 127.0.0.1，
#   然后 sudo systemctl daemon-reload && sudo systemctl restart charger-monitor
#   之后只开放 80/443（防火墙放行 TCP 80、443），8765 不再对外。
